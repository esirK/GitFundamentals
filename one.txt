Branches are entry point of historywq
HEAD:
-> Points to current commit
-> Delete one branch all commits become unreachable
->
INDEX Storage Area/ Staging Area:
Stands b2n working area and repository

BASIC WORKFLOW COMMANDS:
Moving data from working area to index/staging area => git add is used
Moving data from index/staging to repository => git commit
Moving data from repository to index/staging area => git checkout<file>
Moving data from index to working area => git reset <file name>

